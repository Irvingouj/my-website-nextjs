import { createTheme } from '@mui/material';

export const muitheme = createTheme({
  palette: {
    primary: {
      main: '#87cefa',
      contrastText: '#fff',
    },
    secondary: {
      main: '#f3f6fb',
    },
  },
});
